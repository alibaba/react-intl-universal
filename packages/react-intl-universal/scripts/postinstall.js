
try {
  const chalk = require('chalk');
  console.log('');
  console.log(chalk.bold('Efficient way to extract i18n locale messages, use ') + chalk.bold.magenta('react-intl-universal-extract') + ':');
  console.log(chalk.cyan('https://github.com/alibaba/react-intl-universal/tree/master/packages/react-intl-universal-extract'));
  console.log('');
} catch (error) {
  // don't print message even if there's an error
}
